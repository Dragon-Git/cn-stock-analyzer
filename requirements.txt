# 核心依赖
# 钉版策略: 主版本钉住 (避免 break), 次要/补丁留 range 让 Dependabot 提 PR
akquant>=0.3.41,<1.0
akshare>=1.18.91,<2.0

# 数据
pandas>=3.0.5,<4        # 3.0 引入了 CoW 默认, 测试通过
numpy>=2.5.2,<3         # 2.x 大版本

# HTTP
requests>=2.34.2,<3
curl_cffi>=0.16.0,<1
